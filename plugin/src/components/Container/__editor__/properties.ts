import { Editor } from '~/types'

const config: Editor.BaseProperties = {
  style: {
    title: '布局样式',
    type: 'json',
    height: 100,
    defaultValue: {
      padding: '8px',
    },
  },
}

export default config
