---
order: 0
title: 分割线
---

```jsx
import Hetu from 'hetu'

const elementConfig = {
    type: 'HtDivider',
    props: {
      type: 'horizontal',
      title: '分割线',
      orientation: 'center',
    },
    children: [],
}

ReactDOM.render(<Hetu elementConfig={elementConfig} />, mountNode)
```
